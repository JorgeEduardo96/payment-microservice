# Speeds up local and CI builds: lets independent subprojects (client-service,
# order-service, payment-service, etc.) compile/test in parallel, and reuses
# task outputs from the local build cache across `clean build` invocations
# whenever inputs haven't changed.
org.gradle.parallel=true
org.gradle.caching=true
